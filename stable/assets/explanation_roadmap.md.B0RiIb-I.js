import{c as a,S as t,j as r,m as o}from"./chunks/framework.jMzMCdbA.js";const u=JSON.parse('{"title":"Roadmap and ways to contribute","description":"","frontmatter":{},"headers":[],"relativePath":"explanation/roadmap.md","filePath":"explanation/roadmap.md","lastUpdated":null}'),i={name:"explanation/roadmap.md"};function n(s,e,l,c,d,p){return t(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="roadmap" tabindex="-1">Roadmap and ways to contribute <a class="header-anchor" href="#roadmap" aria-label="Permalink to &quot;Roadmap and ways to contribute {#roadmap}&quot;">​</a></h1><p>AIBECS is maintained by a few people. A lot of the machinery is in place, but several directions remain wide open. This page lists the ones we think would have the biggest impact, partly to record where we&#39;d like to take the package, and partly as an invitation: if any of these resonate, please <a href="https://github.com/JuliaOcean/AIBECS.jl/issues" target="_blank" rel="noreferrer">open an issue</a> or <a href="https://github.com/JuliaOcean/AIBECS.jl/pulls" target="_blank" rel="noreferrer">start a PR</a>.</p><h2 id="Parameter-estimation-beyond-the-current-example" tabindex="-1">Parameter estimation beyond the current example <a class="header-anchor" href="#Parameter-estimation-beyond-the-current-example" aria-label="Permalink to &quot;Parameter estimation beyond the current example {#Parameter-estimation-beyond-the-current-example}&quot;">​</a></h2><p>The <a href="/AIBECS.jl/stable/howtos/4_parameter_optimization#parameter-optimization">parameter-optimisation how-to</a> wires AIBECS&#39; parameter struct, F-1&#39;s cached adjoint, a shared <code>LinearSolve.jl</code> cache, and the <code>Optimization.jl</code> frontend together by hand. It works, but it is one of several plausible designs. A few we&#39;d like to see explored:</p><ul><li><p><strong>Probabilistic programming via <a href="https://turinglang.org/" target="_blank" rel="noreferrer">Turing.jl</a>.</strong> AIBECS lets you declare priors with <code>@prior</code> (or by overloading <code>prior</code> directly), and the optimiser in the how-to minimises <code>-log posterior</code> up to a constant. But this is bespoke code and not a careful, expert-written Bayesian workflow. Wiring AIBECS parameters into a Turing <code>@model</code> would unlock full Bayesian inference (MCMC, variational families) over the steady-state solution, with the same priors driving both regimes. The F-1 gradient/Hessian would compose naturally with Turing&#39;s HMC / NUTS samplers. A small worked example fitting a single tracer would be a great first contribution.</p></li><li><p><strong>Lean further on SciML parameter-estimation idioms.</strong> The SciML stack (<a href="https://docs.sciml.ai/SciMLSensitivity/stable/" target="_blank" rel="noreferrer">SciMLSensitivity</a>, <a href="https://docs.sciml.ai/DiffEqParamEstim/stable/" target="_blank" rel="noreferrer">DiffEqParamEstim</a>, <a href="https://docs.sciml.ai/Optimization/stable/" target="_blank" rel="noreferrer">Optimization.jl</a>) has a well-trodden pipeline for fitting <code>ODEProblem</code> / <code>SteadyStateProblem</code> parameters from data. Right now AIBECS bolts <a href="https://github.com/briochemc/F1Method.jl" target="_blank" rel="noreferrer">F1Method.jl</a> onto <code>Optimization</code> by hand. Restructuring the AIBECS objective surface so it slots into SciML&#39;s <code>build_loss_objective</code>-style helpers would let users mix AIBECS models with the rest of the SciML param-fitting ecosystem without bespoke glue.</p></li></ul><h2 id="Simpler-parameter-representation" tabindex="-1">Simpler parameter representation <a class="header-anchor" href="#Simpler-parameter-representation" aria-label="Permalink to &quot;Simpler parameter representation {#Simpler-parameter-representation}&quot;">​</a></h2><p>The current <a href="/AIBECS.jl/stable/reference/functions#AIBECS.AbstractParameters"><code>AbstractParameters</code></a> machinery stacks several macros from <a href="https://github.com/rafaqz/FieldMetadata.jl" target="_blank" rel="noreferrer">FieldMetadata.jl</a> (<code>@units</code>, <code>@prior</code>, <code>@description</code>, <code>@bounds</code>, <code>@logscaled</code>, <code>@flattenable</code>, <code>@reference</code>, plus an <code>@initial_value</code> we define ourselves) on top of a plain struct. It is expressive — every field carries units, a prior, a description, bounds, etc. — but the macro layering has real costs:</p><ul><li><p>Reading and writing a parameter value goes through the FieldMetadata/Flatten plumbing rather than ordinary struct access, which makes it hard to plug AIBECS parameters straight into packages that expect a <code>NamedTuple</code>, <a href="https://github.com/SciML/ComponentArrays.jl" target="_blank" rel="noreferrer"><code>ComponentArrays.jl</code></a>, or a Turing <code>@model</code>.</p></li><li><p>The metadata is attached at the type level via macros, so building a parameter set programmatically (e.g. from a CSV, or generated by another package) is awkward.</p></li><li><p>New contributors have to learn the macro stack before they can add a parameter.</p></li></ul><p>A nice direction would be a flatter representation — one small <code>Parameter</code> value per field carrying <code>value</code>, <code>unit</code>, <code>prior</code>, <code>bounds</code>, <code>logscaled</code>, <code>description</code>, <code>reference</code>, gathered into a <code>NamedTuple</code> or <code>ComponentArray</code>. Standard introspection (<code>propertynames</code>, <code>getproperty</code>) then works without macros, the optimisable subset is a filter rather than a <code>@flattenable</code> annotation, and downstream packages can read/write parameters with no AIBECS-specific glue. The existing pretty-printing, CSV round-tripping, and unit conversions would need to be re-implemented on top, but the surface area shrinks considerably.</p><h2 id="Alternative-sensitivity-backends" tabindex="-1">Alternative sensitivity backends <a class="header-anchor" href="#Alternative-sensitivity-backends" aria-label="Permalink to &quot;Alternative sensitivity backends {#Alternative-sensitivity-backends}&quot;">​</a></h2><p>The how-to uses F1Method because it caches the steady-state factors and exposes both gradient and Hessian. Two natural alternatives that would compose well with the AIBECS objective surface:</p><ul><li><p><a href="https://docs.sciml.ai/SciMLSensitivity/stable/" target="_blank" rel="noreferrer"><code>SciMLSensitivity.SteadyStateAdjoint</code></a> computes a per-call adjoint gradient with no cross-call caching and no Hessian — appropriate when the steady-state solve dominates and you only need the gradient.</p></li><li><p><a href="https://github.com/gdalle/ImplicitDifferentiation.jl" target="_blank" rel="noreferrer">ImplicitDifferentiation.jl</a> provides general implicit-function differentiation; no F-1 Hessian today, but the package is actively developed.</p></li></ul><p>Either would be a meaningful upstream PR — slotting underneath the AIBECS objective / gradient surface and letting users pick the sensitivity strategy at solve time.</p><h2 id="More-circulations" tabindex="-1">More circulations <a class="header-anchor" href="#More-circulations" aria-label="Permalink to &quot;More circulations {#More-circulations}&quot;">​</a></h2><p>AIBECS currently ships OCIM0/1/2/2-48L, OCCA, Archer et al. (2000), the two-box and Primeau 2×2×2 toy circulations, and Haine &amp; Hall (2025). Two natural extensions:</p><ul><li><p><strong>More published transport matrices.</strong> ECCO-derived and CMIP-derived matrices, ACCESS-OM2, MOM6, and any of the offline products coming out of <a href="https://github.com/JuliaOcean/OceanTransportMatrixBuilder.jl" target="_blank" rel="noreferrer">OceanTransportMatrixBuilder.jl</a> would each be a self-contained PR — see the existing <code>OCIM*.jl</code> / <code>OCCA.jl</code> files for the shape (download via <code>DataDeps</code>, expose <code>T</code>, <code>grid</code>, <code>wet3D</code>, and the associated diagnostics).</p></li><li><p><strong>A generic loader.</strong> Most additions today copy an existing <code>OCIM2.jl</code>-style file. A small <code>load_circulation(path)</code> that reads a standard layout (transport operator + grid + masks) would cut the boilerplate and make user-contributed circulations a one-file affair.</p></li></ul><h2 id="Non-ocean-components" tabindex="-1">Non-ocean components <a class="header-anchor" href="#Non-ocean-components" aria-label="Permalink to &quot;Non-ocean components {#Non-ocean-components}&quot;">​</a></h2><p>Several biogeochemical problems need state variables that aren&#39;t 3D ocean tracers:</p><ul><li><p><strong>Slab atmospheres.</strong> A single well-mixed atmospheric reservoir for CO₂ (or O₂) is required to close the carbon cycle — pCO₂(atm) evolves in time and exchanges with surface DIC via air–sea flux. See <a href="https://github.com/briochemc/MatrixMarineCarbonCycleModel/blob/main/src/F_PCO2.m" target="_blank" rel="noreferrer">PCO2 model</a> for an example reference where <code>pCO2atm</code> is carried as a 0D state variable alongside the 3D tracers.</p></li><li><p><strong>Surface-mean intermediates.</strong> Quantities like a surface-mean DIC or ALK (like the <code>DICsrf</code>/<code>ALKsrf</code> variables in the PCO2 model) are useful to apply precipitation/evaporation virtual fluxes consistently across the surface without recomputing the mean inside every right-hand-side call.</p></li><li><p><strong>Other &quot;auxiliary&quot; state.</strong> Sediment pools, ice reservoirs, simple land boxes — anything that couples to the 3D field through a small number of boundary fluxes.</p></li></ul><p>The common requirement is letting AIBECS&#39;s <a href="/AIBECS.jl/stable/reference/functions#AIBECS.AIBECSFunction"><code>AIBECSFunction</code></a> carry state vectors that aren&#39;t shaped like a tracer-on-the-grid, with the Jacobian assembly extended to handle the resulting block-sparse couplings (3D ↔ 2D surface, 3D ↔ 0D atmosphere). Writing the automatic scaffolding for building the Jacobian rows/columns for these different-sized blocks is the bulk of the work.</p><h2 id="Coarsening-via-lump-and-spray" tabindex="-1">Coarsening via lump-and-spray <a class="header-anchor" href="#Coarsening-via-lump-and-spray" aria-label="Permalink to &quot;Coarsening via lump-and-spray {#Coarsening-via-lump-and-spray}&quot;">​</a></h2><p><a href="https://github.com/JuliaOcean/OceanTransportMatrixBuilder.jl" target="_blank" rel="noreferrer">OceanTransportMatrixBuilder.jl</a> provides <code>lump_and_spray(wet3D, vol, T; di, dj, dk)</code> which returns <code>LUMP</code>, <code>SPRAY</code>, and a coarsened volume vector such that <code>LUMP * T * SPRAY</code> is a volume-conserving coarsened transport operator (and <code>LUMP * x</code> / <code>SPRAY * x_c</code> move tracer fields between the two grids). Wiring this into AIBECS so any shipped circulation can be coarsened on demand would unlock fast iteration on parameter sweeps and sensitivity studies before refining on the native grid.</p><h2 id="Iterative-linear-solvers-and-preconditioners" tabindex="-1">Iterative linear solvers and preconditioners <a class="header-anchor" href="#Iterative-linear-solvers-and-preconditioners" aria-label="Permalink to &quot;Iterative linear solvers and preconditioners {#Iterative-linear-solvers-and-preconditioners}&quot;">​</a></h2><p>Steady-state solves currently rely on direct sparse solvers on the CPU <a href="https://docs.sciml.ai/LinearSolve/stable/" target="_blank" rel="noreferrer">LinearSolve.jl</a> (UMFPACK by default). That is the right choice for OCIM-sized problems, but it does not scale well, as both memory and factorisation time grow quickly with increasing resolution, and there is no GPU path (like CUDSS). Krylov-subspace solvers (GMRES, BiCGStab) from <a href="https://github.com/JuliaSmoothOptimizers/Krylov.jl" target="_blank" rel="noreferrer">Krylov.jl</a> or <a href="https://github.com/JuliaLinearAlgebra/IterativeSolvers.jl" target="_blank" rel="noreferrer">IterativeSolvers.jl</a> would address both, but only with effective preconditioning: ILU(0), algebraic multigrid (via <a href="https://github.com/JuliaLinearAlgebra/AlgebraicMultigrid.jl" target="_blank" rel="noreferrer">AlgebraicMultigrid.jl</a>), or physics-based block preconditioners that exploit the advection–diffusion structure. A useful first step is to benchmark a few solver/preconditioner pairs against the existing LU baseline on OCIM2 and the 48-layer variant; from there, exposing the choice through the <code>solve(...; alg=...)</code> interface follows the standard LinearSolve pattern.</p><h2 id="Visualisation" tabindex="-1">Visualisation <a class="header-anchor" href="#Visualisation" aria-label="Permalink to &quot;Visualisation {#Visualisation}&quot;">​</a></h2><ul><li><p>A <strong>GeoMakie-based how-to</strong> (real projections, coastlines, country borders) on top of the <a href="/AIBECS.jl/stable/howtos/2b_plot_makie#plots-makie">existing Makie how-to</a> — the current guide deliberately stays on Cartesian <code>Axis</code> to keep the dependency footprint small.</p></li><li><p><strong>Makie recipes</strong> to mirror the existing Plots.jl recipes (<code>plothorizontalslice</code>, <code>plotzonalmean</code>, <code>plotdepthprofile</code>, …) so Makie users don&#39;t have to compute slices / means by hand.</p></li></ul><h2 id="General" tabindex="-1">General <a class="header-anchor" href="#General" aria-label="Permalink to &quot;General {#General}&quot;">​</a></h2><ul><li><p>More <strong>how-tos</strong>, especially around coupling AIBECS to external forcings, working with custom grids, or porting a published model.</p></li><li><p>More <strong>tutorials</strong>: anything that walks through a real published setup end-to-end will help new users a lot.</p></li><li><p><strong>Performance</strong>: profiling reports, faster sparse Jacobian construction, GPU experiments — all welcome (see also the solver/preconditioner section above).</p></li></ul><p>If you have an idea that doesn&#39;t fit any of the buckets above, that&#39;s also fine; please open an issue first and we&#39;ll figure out the right shape together.</p>',29)])])}const m=a(i,[["render",n]]);export{u as __pageData,m as default};
