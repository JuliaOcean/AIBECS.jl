import{c as a,S as t,j as o,m as r}from"./chunks/framework.BowGar06.js";const u=JSON.parse('{"title":"Roadmap and ways to contribute","description":"","frontmatter":{},"headers":[],"relativePath":"roadmap.md","filePath":"roadmap.md","lastUpdated":null}'),i={name:"roadmap.md"};function n(s,e,l,c,d,h){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="roadmap" tabindex="-1">Roadmap and ways to contribute <a class="header-anchor" href="#roadmap" aria-label="Permalink to &quot;Roadmap and ways to contribute {#roadmap}&quot;">​</a></h1><p>AIBECS is maintained by a few people. A lot of the machinery is in place, but several directions remain wide open. This page lists the ones we think would have the biggest impact, partly to record where we&#39;d like to take the package, and partly as an invitation: if any of these resonate, please <a href="https://github.com/JuliaOcean/AIBECS.jl/issues" target="_blank" rel="noreferrer">open an issue</a> or <a href="https://github.com/JuliaOcean/AIBECS.jl/pulls" target="_blank" rel="noreferrer">start a PR</a>.</p><h2 id="Parameter-estimation-beyond-the-current-example" tabindex="-1">Parameter estimation beyond the current example <a class="header-anchor" href="#Parameter-estimation-beyond-the-current-example" aria-label="Permalink to &quot;Parameter estimation beyond the current example {#Parameter-estimation-beyond-the-current-example}&quot;">​</a></h2><p>The <a href="/AIBECS.jl/stable/howtos/parameter_optimization#parameter-optimization">parameter-optimisation how-to</a> wires AIBECS&#39; parameter struct, F-1&#39;s cached adjoint, a shared <code>LinearSolve.jl</code> cache, and the <code>Optimization.jl</code> frontend together by hand. It works, but it is one of several plausible designs. A few we&#39;d like to see explored:</p><ul><li><p><strong>Probabilistic programming via <a href="https://turinglang.org/" target="_blank" rel="noreferrer">Turing.jl</a>.</strong> AIBECS lets you declare priors with <code>@prior</code> (or by overloading <code>prior</code> directly), and the optimiser in the how-to minimises <code>-log posterior</code> up to a constant. But this is bespoke code and not a careful, expert-written Bayesian workflow. Wiring AIBECS parameters into a Turing <code>@model</code> would unlock full Bayesian inference (MCMC, variational families) over the steady-state solution, with the same priors driving both regimes. The F-1 gradient/Hessian would compose naturally with Turing&#39;s HMC / NUTS samplers. A small worked example fitting a single tracer would be a great first contribution.</p></li><li><p><strong>Lean further on SciML parameter-estimation idioms.</strong> The SciML stack (<a href="https://docs.sciml.ai/SciMLSensitivity/stable/" target="_blank" rel="noreferrer">SciMLSensitivity</a>, <a href="https://docs.sciml.ai/DiffEqParamEstim/stable/" target="_blank" rel="noreferrer">DiffEqParamEstim</a>, <a href="https://docs.sciml.ai/Optimization/stable/" target="_blank" rel="noreferrer">Optimization.jl</a>) has a well-trodden pipeline for fitting <code>ODEProblem</code> / <code>SteadyStateProblem</code> parameters from data. Right now AIBECS bolts <a href="https://github.com/briochemc/F1Method.jl" target="_blank" rel="noreferrer">F1Method.jl</a> onto <code>Optimization</code> by hand. Restructuring the AIBECS objective surface so it slots into SciML&#39;s <code>build_loss_objective</code>-style helpers would let users mix AIBECS models with the rest of the SciML param-fitting ecosystem without bespoke glue.</p></li></ul><h2 id="Alternative-sensitivity-backends" tabindex="-1">Alternative sensitivity backends <a class="header-anchor" href="#Alternative-sensitivity-backends" aria-label="Permalink to &quot;Alternative sensitivity backends {#Alternative-sensitivity-backends}&quot;">​</a></h2><p>The how-to uses F1Method because it caches the steady-state factors and exposes both gradient and Hessian. Two natural alternatives that would compose well with the AIBECS objective surface:</p><ul><li><p><a href="https://docs.sciml.ai/SciMLSensitivity/stable/" target="_blank" rel="noreferrer"><code>SciMLSensitivity.SteadyStateAdjoint</code></a> computes a per-call adjoint gradient with no cross-call caching and no Hessian — appropriate when the steady-state solve dominates and you only need the gradient.</p></li><li><p><a href="https://github.com/gdalle/ImplicitDifferentiation.jl" target="_blank" rel="noreferrer">ImplicitDifferentiation.jl</a> provides general implicit-function differentiation; no F-1 Hessian today, but the package is actively developed.</p></li></ul><p>Either would be a meaningful upstream PR — slotting underneath the AIBECS objective / gradient surface and letting users pick the sensitivity strategy at solve time.</p><h2 id="Simpler-parameter-representation" tabindex="-1">Simpler parameter representation <a class="header-anchor" href="#Simpler-parameter-representation" aria-label="Permalink to &quot;Simpler parameter representation {#Simpler-parameter-representation}&quot;">​</a></h2><p>The current <a href="/AIBECS.jl/stable/reference/functions#AIBECS.AbstractParameters"><code>AbstractParameters</code></a> machinery stacks several macros from <a href="https://github.com/rafaqz/FieldMetadata.jl" target="_blank" rel="noreferrer">FieldMetadata.jl</a> (<code>@units</code>, <code>@prior</code>, <code>@description</code>, <code>@bounds</code>, <code>@logscaled</code>, <code>@flattenable</code>, <code>@reference</code>, plus an <code>@initial_value</code> we define ourselves) on top of a plain struct. It is expressive — every field carries units, a prior, a description, bounds, etc. — but the macro layering has real costs:</p><ul><li><p>Reading and writing a parameter value goes through the FieldMetadata/Flatten plumbing rather than ordinary struct access, which makes it hard to plug AIBECS parameters straight into packages that expect a <code>NamedTuple</code>, <a href="https://github.com/SciML/ComponentArrays.jl" target="_blank" rel="noreferrer"><code>ComponentArrays.jl</code></a>, or a Turing <code>@model</code>.</p></li><li><p>The metadata is attached at the type level via macros, so building a parameter set programmatically (e.g. from a CSV, or generated by another package) is awkward.</p></li><li><p>New contributors have to learn the macro stack before they can add a parameter.</p></li></ul><p>A nice direction would be a flatter representation — one small <code>Parameter</code> value per field carrying <code>value</code>, <code>unit</code>, <code>prior</code>, <code>bounds</code>, <code>logscaled</code>, <code>description</code>, <code>reference</code>, gathered into a <code>NamedTuple</code> or <code>ComponentArray</code>. Standard introspection (<code>propertynames</code>, <code>getproperty</code>) then works without macros, the optimisable subset is a filter rather than a <code>@flattenable</code> annotation, and downstream packages can read/write parameters with no AIBECS-specific glue. The existing pretty-printing, CSV round-tripping, and unit conversions would need to be re-implemented on top, but the surface area shrinks considerably.</p><h2 id="More-circulations" tabindex="-1">More circulations <a class="header-anchor" href="#More-circulations" aria-label="Permalink to &quot;More circulations {#More-circulations}&quot;">​</a></h2><p>AIBECS currently ships OCIM0/1/2/2-48L, OCCA, Archer et al. (2000), the two-box and Primeau 2×2×2 toy circulations, and Haine &amp; Hall (2025). Two natural extensions:</p><ul><li><p><strong>More published transport matrices.</strong> ECCO-derived and CMIP-derived matrices, ACCESS-OM2, MOM6, and any of the offline products coming out of <a href="https://github.com/TMIP-code/OceanTransportMatrixBuilder.jl" target="_blank" rel="noreferrer">OceanTransportMatrixBuilder.jl</a> would each be a self-contained PR — see the existing <code>OCIM*.jl</code> / <code>OCCA.jl</code> files for the shape (download via <code>DataDeps</code>, expose <code>T</code>, <code>grid</code>, <code>wet3D</code>, and the associated diagnostics).</p></li><li><p><strong>A generic loader.</strong> Most additions today copy an existing <code>OCIM2.jl</code>-style file. A small <code>load_circulation(path)</code> that reads a standard layout (transport operator + grid + masks) would cut the boilerplate and make user-contributed circulations a one-file affair.</p></li></ul><h2 id="Regional-circulation-models" tabindex="-1">Regional circulation models <a class="header-anchor" href="#Regional-circulation-models" aria-label="Permalink to &quot;Regional circulation models {#Regional-circulation-models}&quot;">​</a></h2><p>All circulations shipped today are global. Ingesting <em>natively regional</em> transport matrices — e.g. from regional MOM6/ROMS runs with explicit open boundary conditions — would let AIBECS run biogeochemistry on a subdomain at much finer resolution than any global matrix can offer. <a href="https://doi.org/10.5194/gmd-16-2795-2023" target="_blank" rel="noreferrer">Schoonover et al. (2023)</a> is a good reference for the kind of regional (and high-resolution) offline transport this would target. The main scaffolding work is generalising boundary-condition handling so a prescribed open-boundary inflow (possibly time-varying) becomes a first-class concept alongside the existing surface and sediment boundaries.</p><h2 id="High-resolution-circulation-models" tabindex="-1">High-resolution circulation models <a class="header-anchor" href="#High-resolution-circulation-models" aria-label="Permalink to &quot;High-resolution circulation models {#High-resolution-circulation-models}&quot;">​</a></h2><p>Whether AIBECS itself should handle eddy-resolving (¼° or finer) global circulations is an open question — at those resolutions direct factorisation is out of reach and GPU-resident iterative solvers become mandatory (see also the iterative-solvers section). The <a href="https://github.com/TMIP-code/ACCESS-OM2_x_Oceananigans" target="_blank" rel="noreferrer">ACCESS-OM2 × Oceananigans</a> project demonstrates feasibility: GPU + iterative solves of single tracers at ¼° in a periodic setting, with a target of 0.1° × 75 levels — roughly 1200× the size of OCIM2. Even if the full eddy-resolving regime ends up living outside AIBECS, the choices made there (matrix storage, solver/preconditioner pairs, GPU offload) should feed back into AIBECS once iterative solvers land.</p><h2 id="Coarsening-via-lump-and-spray" tabindex="-1">Coarsening via lump-and-spray <a class="header-anchor" href="#Coarsening-via-lump-and-spray" aria-label="Permalink to &quot;Coarsening via lump-and-spray {#Coarsening-via-lump-and-spray}&quot;">​</a></h2><p><a href="https://github.com/TMIP-code/OceanTransportMatrixBuilder.jl" target="_blank" rel="noreferrer">OceanTransportMatrixBuilder.jl</a> provides <code>lump_and_spray(wet3D, vol, T; di, dj, dk)</code> which returns <code>LUMP</code>, <code>SPRAY</code>, and a coarsened volume vector such that <code>LUMP * T * SPRAY</code> is a volume-conserving coarsened transport operator (and <code>LUMP * x</code> / <code>SPRAY * x_c</code> move tracer fields between the two grids). Wiring this into AIBECS so any shipped circulation can be coarsened on demand would unlock fast iteration on parameter sweeps and sensitivity studies before refining on the native grid.</p><h2 id="Non-ocean-components" tabindex="-1">Non-ocean components <a class="header-anchor" href="#Non-ocean-components" aria-label="Permalink to &quot;Non-ocean components {#Non-ocean-components}&quot;">​</a></h2><p>Several biogeochemical problems need state variables that aren&#39;t 3D ocean tracers:</p><ul><li><p><strong>Slab atmospheres.</strong> A single well-mixed atmospheric reservoir for CO₂ (or O₂) is required to close the carbon cycle — pCO₂(atm) evolves in time and exchanges with surface DIC via air–sea flux. See <a href="https://github.com/briochemc/MatrixMarineCarbonCycleModel/blob/main/src/F_PCO2.m" target="_blank" rel="noreferrer">PCO2 model</a> for an example reference where <code>pCO2atm</code> is carried as a 0D state variable alongside the 3D tracers.</p></li><li><p><strong>Surface-mean intermediates.</strong> Quantities like a surface-mean DIC or ALK (like the <code>DICsrf</code>/<code>ALKsrf</code> variables in the PCO2 model) are useful to apply precipitation/evaporation virtual fluxes consistently across the surface without recomputing the mean inside every right-hand-side call.</p></li><li><p><strong>Other &quot;auxiliary&quot; state.</strong> Sediment pools, ice reservoirs, simple land boxes — anything that couples to the 3D field through a small number of boundary fluxes.</p></li></ul><p>The common requirement is letting AIBECS&#39;s <a href="/AIBECS.jl/stable/reference/functions#AIBECS.AIBECSFunction"><code>AIBECSFunction</code></a> carry state vectors that aren&#39;t shaped like a tracer-on-the-grid, with the Jacobian assembly extended to handle the resulting block-sparse couplings (3D ↔ 2D surface, 3D ↔ 0D atmosphere). Writing the automatic scaffolding for building the Jacobian rows/columns for these different-sized blocks is the bulk of the work.</p><h2 id="Iterative-linear-solvers-and-preconditioners" tabindex="-1">Iterative linear solvers and preconditioners <a class="header-anchor" href="#Iterative-linear-solvers-and-preconditioners" aria-label="Permalink to &quot;Iterative linear solvers and preconditioners {#Iterative-linear-solvers-and-preconditioners}&quot;">​</a></h2><p>Steady-state solves currently rely on direct sparse solvers on the CPU <a href="https://docs.sciml.ai/LinearSolve/stable/" target="_blank" rel="noreferrer">LinearSolve.jl</a> (UMFPACK by default). That is the right choice for OCIM-sized problems, but it does not scale well, as both memory and factorisation time grow quickly with increasing resolution, and there is no GPU path. Nothing in the call chain — <code>SteadyStateProblem</code> → AIBECS state function → linear solve — is inherently CPU-only, so a GPU-aware factorisation like <a href="https://github.com/exanauts/CUDSS.jl" target="_blank" rel="noreferrer">CUDSS.jl</a> should in principle drop in, but this path has not been exercised. Krylov-subspace solvers (GMRES, BiCGStab) from <a href="https://github.com/JuliaSmoothOptimizers/Krylov.jl" target="_blank" rel="noreferrer">Krylov.jl</a> or <a href="https://github.com/JuliaLinearAlgebra/IterativeSolvers.jl" target="_blank" rel="noreferrer">IterativeSolvers.jl</a> would address both, but only with effective preconditioning: ILU(0), algebraic multigrid (via <a href="https://github.com/JuliaLinearAlgebra/AlgebraicMultigrid.jl" target="_blank" rel="noreferrer">AlgebraicMultigrid.jl</a>), or physics-based block preconditioners that exploit the advection–diffusion structure. A useful first step is to benchmark a few solver/preconditioner pairs against the existing LU baseline on OCIM2 and the 48-layer variant; from there, exposing the choice through the <code>solve(...; alg=...)</code> interface follows the standard LinearSolve pattern.</p><h2 id="Time-stepping" tabindex="-1">Time stepping <a class="header-anchor" href="#Time-stepping" aria-label="Permalink to &quot;Time stepping {#Time-stepping}&quot;">​</a></h2><p>AIBECS is built around steady-state solves. A first-class time-stepping interface — integrating the same <code>AIBECSFunction</code> forward in time via the SciML ODE stack — would unlock transient runs, spin-up diagnostics, seasonal forcings, and natural coupling with non-steady components (slab atmospheres, sediments). Open design questions: how to expose the transport split (linear T + nonlinear sources) cleanly to IMEX schemes, how to share Jacobian assembly with the existing steady-state solver, and how to use a precomputed steady state as the initial condition. This entry is meant to open a discussion, not prescribe a design.</p><h2 id="Pseudo-transient-solvers-for-stiff-problems" tabindex="-1">Pseudo-transient solvers for stiff problems <a class="header-anchor" href="#Pseudo-transient-solvers-for-stiff-problems" aria-label="Permalink to &quot;Pseudo-transient solvers for stiff problems {#Pseudo-transient-solvers-for-stiff-problems}&quot;">​</a></h2><p>Newton&#39;s method on <code>F(x) = 0</code> works well when the initial guess is good and the Jacobian is well-conditioned, but stiff source/sink terms, strongly nonlinear closures, or multiple co-existing equilibria can make it stall or pick the wrong branch. A robust fallback is <em>pseudo-transient continuation</em>: integrate <code>dx/dt = F(x)</code> toward steady state with an implicit scheme whose time step starts small and grows as the residual drops, approaching a Newton step in the limit. <a href="https://github.com/ctkelley/SIAMFANLEquations.jl" target="_blank" rel="noreferrer">SIAMFANLEquations.jl</a> already implements it (and Newton-Krylov cousins) and is a natural backend; SciML&#39;s <code>DynamicSS</code> family is another option. Either could slot in as an alternative algorithm under <code>solve(SteadyStateProblem(...))</code>.</p><h2 id="Periodic-seasonally-cyclic-solvers" tabindex="-1">Periodic (seasonally cyclic) solvers <a class="header-anchor" href="#Periodic-seasonally-cyclic-solvers" aria-label="Permalink to &quot;Periodic (seasonally cyclic) solvers {#Periodic-seasonally-cyclic-solvers}&quot;">​</a></h2><p>Steady-state solves assume time-invariant forcing. A complementary regime — much cheaper than fully time-stepping — is the <em>periodic</em> steady state: given a seasonally-cyclic transport (e.g. 12 monthly matrices) and seasonally-cyclic forcings, solve directly for the periodic orbit. Two references worth building on:</p><ul><li><p><strong>CYCLOCIM</strong> (<a href="https://doi.org/10.1016/j.ocemod.2021.101762" target="_blank" rel="noreferrer">Huang, Primeau &amp; DeVries, 2021</a>) — a 4-D variational assimilation system for the climatological seasonal cycle of the ocean circulation, producing 12 monthly transport matrices.</p></li><li><p><a href="https://doi.org/10.1029/2025GL116799" target="_blank" rel="noreferrer">Pasquier et al. (2025)</a> — uses a periodic-solve framework to compute deep-ocean transit-time distributions and sequestration efficiency offline from climate-model archives.</p></li></ul><p>A <code>solve_periodic</code> interface alongside the existing steady-state path (and any future time-stepping path) would open seasonally-resolved BGC studies that currently force users into long transient integrations.</p><h2 id="Circulation-diagnostic-tutorials" tabindex="-1">Circulation diagnostic tutorials <a class="header-anchor" href="#Circulation-diagnostic-tutorials" aria-label="Permalink to &quot;Circulation diagnostic tutorials {#Circulation-diagnostic-tutorials}&quot;">​</a></h2><p>AIBECS already has everything needed to diagnose a circulation; three short tutorials would make this much more visible:</p><ul><li><p><strong>Water-mass fractions.</strong> Solve for a passive tracer with a Dirichlet/restoring boundary condition (= 1 in a chosen surface region, = 0 elsewhere); the steady interior values give the fraction of each gridcell last in contact with the surface in that region.</p></li><li><p><strong>Adjoint tracers and mean time to reemergence.</strong> Build the true adjoint transport <code>V⁻¹ Tᵀ V</code> (the adjoint with respect to the volume-weighted inner product <code>⟨x, y⟩ = xᵀ V y</code>, the discrete equivalent of <code>∫ x y dV</code>), with a unit source everywhere and a fast surface relaxation as sink; the steady solution is the mean time to reemergence. The forward analogue with the same source/sink gives the ideal age.</p></li><li><p><strong>Ventilation per unit area.</strong> From the age <code>a</code> and reemergence-time <code>r</code> fields, compute <code>calV↑ = vol · a · 1_surf / area</code> and <code>calV↓ = vol · r · 1_surf / area</code>. <a href="https://doi.org/10.1029/2024JC021043" target="_blank" rel="noreferrer">Pasquier et al. (2024)</a> uses <code>calV↓</code> to diagnose deoxygenation pathways.</p></li></ul><h2 id="Visualisation" tabindex="-1">Visualisation <a class="header-anchor" href="#Visualisation" aria-label="Permalink to &quot;Visualisation {#Visualisation}&quot;">​</a></h2><ul><li><p>A <strong>GeoMakie-based how-to</strong> (real projections, coastlines, country borders) on top of the <a href="/AIBECS.jl/stable/howtos/plot_makie#plots-makie">existing Makie how-to</a> — the current guide deliberately stays on Cartesian <code>Axis</code> to keep the dependency footprint small.</p></li><li><p><strong>Makie recipes</strong> to mirror the existing Plots.jl recipes (<code>plothorizontalslice</code>, <code>plotzonalmean</code>, <code>plotdepthprofile</code>, …) so Makie users don&#39;t have to compute slices / means by hand.</p></li></ul><h2 id="General" tabindex="-1">General <a class="header-anchor" href="#General" aria-label="Permalink to &quot;General {#General}&quot;">​</a></h2><ul><li><p>More <strong>how-tos</strong>, especially around coupling AIBECS to external forcings, working with custom grids, or porting a published model.</p></li><li><p>More <strong>tutorials</strong>: anything that walks through a real published setup end-to-end will help new users a lot.</p></li><li><p><strong>Performance</strong>: profiling reports, faster sparse Jacobian construction, GPU experiments — all welcome (see also the solver/preconditioner section above).</p></li></ul><p>If you have an idea that doesn&#39;t fit any of the buckets above, that&#39;s also fine; please open an issue first and we&#39;ll figure out the right shape together.</p>',44)])])}const m=a(i,[["render",n]]);export{u as __pageData,m as default};
